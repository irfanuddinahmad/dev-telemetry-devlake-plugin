module github.com/YOUR_USERNAME/dev-telemetry-devlake-plugin

go 1.21

// This go.mod is for reference only. The plugin must be built from within
// the DevLake monorepo as it depends on DevLake's internal packages.
//
// When building, the DevLake monorepo's go.mod will be used instead.
